{
  "name": "propeller-api-e2e-tests",
  "version": "1.0.0",
  "description": "E2E automated tests for the Propeller E-Commerce GraphQL API",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "jest --verbose --runInBand",
    "test:watch": "jest --watch --runInBand",
    "test:coverage": "jest --coverage --runInBand"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.17.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
